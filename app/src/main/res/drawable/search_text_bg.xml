<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Background color -->
    <corners android:radius="16dp"/> <!-- Corner radius for the curves -->
    <stroke android:width="2dp" android:color="@android:color/darker_gray"/> <!-- Border width and color -->
    <padding android:left="8dp" android:top="8dp" android:right="8dp" android:bottom="8dp"/> <!-- Padding inside the shape -->
</shape>
